# 安全说明

本仓库只接受公开分流规则，不应包含订阅、节点、密码、令牌、UUID、Cookie 或其他凭据。

- 自定义规则放在 `sources/custom/`，提交前检查其中的 URL 查询参数。
- 私人订阅和客户端配置请保留在其他私有目录，不要复制到本仓库。
- 如果凭据曾进入 Git 历史，仅删除当前文件并不充分；请立即轮换凭据，并清理历史后再公开仓库。

发现敏感信息时，请不要在公开 Issue 中粘贴原文。
